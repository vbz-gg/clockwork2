export {
  compareToRecording,
  decodeRecording,
  diffSnapshots,
  encodeRecording,
  type RecordingComparison,
} from "./codec"
export { RECORDING_FORMAT, RECORDING_VERSION, type Recording } from "./types"
